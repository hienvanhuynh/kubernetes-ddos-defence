version: "3.3"
services:
  ddos-detector:
    image: "${REGISTRY}/ddos-detector"
    container_name: ddos-detector
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    network_mode: "host"